{
  "name": "babel-plugin-dispatch-call",
  "version": "1.0.0",
  "description": "Naive Redux helper to compile dispatch(fn(args)) into dispatch(call(fn, args))",
  "author": "henghonglee <henghonglee@gmail.com>",
  "main": "lib/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/henghonglee/babel-plugin-dispatch-call.git"
  },
  "devDependencies": {
    "babel-cli": "^6.4.5",
    "babel-preset-es2015": "^6.3.13",
    "babel-preset-stage-0": "^6.3.13",
    "mocha": "^2.2.5"
  },
  "scripts": {
    "clean": "rm -rf lib",
    "build": "babel src -d lib",
    "test": "mocha --compilers js:babel-register",
    "test:watch": "npm run test -- --watch",
    "prepublish": "npm run clean && npm run build"
  },
  "keywords": [
    "babel-plugin"
  ]
}
